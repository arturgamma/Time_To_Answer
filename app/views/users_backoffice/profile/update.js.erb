$('#myModal').modal('hide');

document.getElementById('imgAvatarLg').src = "<%= @user.user_profile.avatar.url %>";
document.getElementById('imgAvatarEx').src = "<%= @user.user_profile.avatar.url %>";
document.getElementById('imgAvatarSm').src = "<%= @user.user_profile.avatar.url %>";

$.bootstrapGrowl('Avatar Atualizado com sucesso!!', {
    //ele: 'body', // which element to append to
    type: 'info', // (null, 'info', 'danger', 'success')
    //offset: {from: 'top', amount: 20}, // 'top', or 'bottom'
    align: 'center', // ('left', 'right', or 'center')
    width: 280, // (integer, or 'auto')
    delay: 3000, // Time while the message will be displayed. It's not equivalent to the *demo* timeOut!
    allow_dismiss: true, // If true then will display a cross to close the popup.
    stackup_spacing: 10 // spacing between consecutively stacked growls.
});